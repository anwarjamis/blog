<div class="container">
  <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
    <p class="col-md-4 mb-0 text-muted">Con 🤍 por <strong>Anwar Jamis</strong></p>

    <ul class="nav col-md-4 justify-content-end">
    <% if user_signed_in? %>
      <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link px-2 text-muted" %>
      <%= link_to "Categorías", categories_path, class: "nav-link px-2 text-muted" %>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "nav-link px-2 text-muted" %>
    <% end %>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-muted">Blog</a></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-muted">Home</a></li>
    </ul>
  </footer>
</div>

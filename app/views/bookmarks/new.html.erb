<h1>New bookmark</h1>
<%= simple_form_for [@category, @bookmark] do |f| %>
  <%= f.association :post, collection: Post.order(date: :desc) %>
  <%= f.association :category %>
  <%= f.button :submit %>
<% end %>
